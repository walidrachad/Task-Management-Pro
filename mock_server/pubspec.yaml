name: mock_server
description: Simple mock server for Task Management Pro Codex.
version: 1.0.0
environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5

dev_dependencies:
  lints: ^3.0.0
