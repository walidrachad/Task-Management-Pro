import 'package:flutter/material.dart';

/// Collection of color constants that can be reused across the app's UI.
class AppColors {
  /// Primary brand color used for key actions and highlights.
  static const Color primary = Color(0xFF3F51B5);

  /// Secondary accent color for complementary elements.
  static const Color secondary = Color(0xFFFFC107);

  /// Default background color applied to scaffolds and surfaces.
  static const Color background = Color(0xFFF5F5F5);

  /// Color used to represent error states.
  static const Color error = Color(0xFFD32F2F);

  /// Color used to represent success states.
  static const Color success = Color(0xFF2E7D32);

  /// Default text color for high emphasis content.
  static const Color text = Color(0xFF212121);
}
